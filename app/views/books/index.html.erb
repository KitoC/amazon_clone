
<div style="margin-left: 30px;">

  <h1>Books</h1>
  <% @books.each do |book| %>

  <div style="width: 25px;"> <%= link_to (image_tag(book.image, options={ height: "100px"})), book %> </div>
  <h3 style=""><%= link_to book.name, book %></h3>
  <p style=""><%= book.author.name %></P>

  <% end %>

</div>
<p><%= link_to 'Back', :back %></p>
